<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="estate_property_offer_form_view" model="ir.ui.view">
        <field name="name">estate.property.offer.form</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <form string="Property Offer">
                <sheet>
                    <group>
                        <field name="price"/>
                        <field name="partner_id"/>
                        <field name="validity"/>
                        <field name="date_deadline"/>
                        <field name="property_id"/>
                    </group>
                </sheet>
            </form>
        </field>

    </record>

    <record id="estate_property_offer_list_view" model="ir.ui.view">
        <field name="name">estate.property.offer.list</field>
        <field name="model">estate.property.offer</field>
        <field name="arch" type="xml">
            <list string="Property Offers" editable="bottom" decoration-danger="status=='refused'" decoration-success="status=='accepted'">
                <field name="price"/>
                <field name="partner_id"/>
                <field name="validity"/>
                <field name="date_deadline"/>
                <field name="property_id"/>
                <field name="property_type_id"/>
                <button name="action_accept_offer" string="Accept Offer" type="object" class="btn-success" invisible="status == 'accepted'"/>
                <button name="action_refuse_offer" string="Refuse Offer" type="object" class="btn-danger" invisible="status == 'accepted'"/>

                <field name="status"/>

            </list>
        </field>
    </record>

    <record id="action_estate_property_offer" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">estate.property.offer</field>
        <field name="view_mode">list,form</field>
        
    </record>

</odoo>