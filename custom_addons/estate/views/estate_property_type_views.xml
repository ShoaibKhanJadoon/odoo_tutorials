<?xml version='1.0' encoding='utf-8'?>
<odoo>
    <record id="estate_property_type_form_view" model="ir.ui.view">
        <field name="name">estate.property.type.form</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <form string="Property Type">
                <header>
                    <button name="%(action_estate_property_offer)d" domain="[('property_type_id', '=', id)]" context="{'search_default_property_type_id': id}"> type="action" string="Offers" icon="fa-handshake-o" class="oe_stat_button">
                        <field name="offer_count" widget="statinfo"/>
                    </button>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="description"/>
                    </group>
                    <!-- Inline list view -->
                    <field name="property_ids" string='Properties'>
                        <list>
                            <field name="name"/>
                            <field name="expected_price"/>
                            <field name="state"/>

                        </list>
                    </field>
                </sheet>
            </form>
        </field>

    </record>

    <record id="estate_property_type_list_view" model="ir.ui.view">
        <field name="name">estate.property.type.list</field>
        <field name="model">estate.property.type</field>
        <field name="arch" type="xml">
            <list string="Property Types">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="properties_count"/>
                <field name="offer_count"/>
            </list>
        </field>

    </record>

    <record id="action_estate_property_type" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>

    </record>
</odoo>