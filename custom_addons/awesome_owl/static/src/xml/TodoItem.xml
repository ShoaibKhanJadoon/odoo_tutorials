<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="awesome_owl.TodoItem">
        <div class="border rounded p-2 mb-2" t-att-class="props.todo.isCompleted ? 'text-muted text-decoration-line-through' : ''">
            <input
            type="checkbox"
            t-att-checked="props.todo.isCompleted"
            t-on-change="onToggle"
        />
            <strong>#
                <t t-out="props.todo.id"/>
            </strong>
            <span class="ms-2" t-out="props.todo.description"/>
            <span class="fa fa-remove text-danger ms-auto"
              role="button"
              t-on-click="onRemove"/>
        </div>
    </t>
</templates>