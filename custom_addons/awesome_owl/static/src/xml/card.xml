<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="awesome_owl.Card">
        <div class="card d-inline-block m-2" style="width: 18rem;">
            <div class="card-body">
                <t t-if="props.title">
                    <h5 class="card-title" t-out="props.title"/>
                </t>
                <t t-if="props.content">
                    <p class="card-text" t-out="props.content"/>
                </t>
                
                    <button class="btn btn-sm btn-outline-secondary" t-on-click="toggle">
                        <t t-if="state.isOpen">−</t>
                        <t t-if="!state.isOpen">+</t>
                    </button>

                <div class="card-body" t-if="state.isOpen">
                    <t t-slot="default"/>
                </div>
            </div>
        </div>
    </t>

</templates>
